module.exports = {
	form: {
		name:           element(by.id('name')),
		favoriteColor:  element(by.id('favColor')),
		submitButton:   element(by.id('formButton')),
		response:       element(by.id('formResponse'))
	}
}